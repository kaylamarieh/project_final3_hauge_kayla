$(document).ready(function () {

    //loads the elements onto the page on scroll
    $('.load').boxLoader({
        direction: 'y',
        position: '100%',
        effect: 'fadeIn',
        duration: '0.5s',
        windowarea: '50%'
    });
});
